%div{:class=>'recd-friend-request',:id=>"recd-req-#{friendship.id}"}
  %p
    "Hey,
    = friendship.friend.full_name
    wanna be my friend?" -
    = friendship.member.full_name
  %p
    = link_to 'Accept', {:controller => 'friendships', :action => 'accept', :id => friendship.id}, :remote => true
    = link_to 'Deny', {:controller => 'friendships', :action => 'deny', :id => friendship.id}, :remote => true