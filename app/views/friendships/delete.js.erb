$("div.default-dialog").dialog({
    title:'Delete Friend',
    show: { effect: "fold", options: {}, speed: 150 },
    hide: { effect: "explode", options: {}, speed: 150 },
    modal:true,
    buttons: {
        Ok: function () {
            $(this).dialog("close");

            <% if @success %>
                $("#friend-req-<%= @friendship.id %>").hide("drop", { direction: "down" }, 1000);
            <% end %>
        }
    }
});

<% if @success %>
    $("div.default-dialog").html('<%= @friendship.member.full_name %> has been deleted');
<% else %>
    $("div.default-dialog").html('Could not delete <%= @friendship.member.full_name %>');
<% end %>