$("div.default-dialog").dialog({
    title:'Member Request',
    show: { effect: "fold", options: {}, speed: 150 },
    hide: { effect: "explode", options: {}, speed: 150 },
    modal:true,
    buttons: {
        Ok: function () {
            $(this).dialog("close");

            <% if @success %>
                $("#sent-member-req-<%= @member.id %>").hide("drop", { direction: "up" }, 1000);
            <% end %>
        }
    }
});

<% if @success %>
    $("div.default-dialog").html('Sent a Member Request to <%= @member.full_name %>');
<% else %>
    $("div.default-dialog").html('Could not send Member Request to <%= if @member then @member.full_name else 'N/A' end %>: <%= @message %>');
<% end %>