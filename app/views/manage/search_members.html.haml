- if @members.any?
  - @members.each do |m|
    %div{:class=>"simple_info"}
      %p
        %b= 'Member #' + m.id.to_s + ': '
        = m.full_name + ': '
        %a{:href => '/manage/member/' + m.id.to_s } 'Edit'
- else
  %p
    No Results
